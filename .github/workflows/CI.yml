name: CI
on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
jobs:
  react_native_expo:
    name: CI
    uses: GabrielSoares-Dev/workflows/.github/workflows/react-native-expo-CI.yml@master
    with:
      node_version: '22.x'